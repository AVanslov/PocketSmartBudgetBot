version: '3'

volumes:
  pg-data:

services:
  db:
    image: postgres:13.10
    env_file: .env
    volumes:
      - pg_data:/var/lib/postgresql/data

  backend:
    image: avanslov/budget_project
    env_file: .env
    depends_on:
      - db
      