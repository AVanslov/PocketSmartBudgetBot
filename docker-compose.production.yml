# version: '3'

volumes:
  db:

services:
  db:
    image: postgres:13.10
    env_file: .env
    volumes:
      - db:/var/lib/postgresql/data

  backend:
    image: avanslov/budget_project
    env_file: .env
    depends_on:
      - db
      